# Mathematical formulas and expressions

## Inline
This is an example of an inline mathematical expression:$f(k) = {n \choose k} p^{k} (1-p)^{n-k}$

## Paragraphed
This is an example of a formula that starts on a new paragraph:
$$f(k) = {n \choose k} p^{k} (1-p)^{n-k}$$

## Multi step
This is an example of a multiline expression:

\begin{equation}
f\left(k\right)=\binom{n}{k}p^k\left(1-p\right)^{n-k}\\
E=mc^2\\
a^2 = b2 + c^2
(\#eq:binom)
\end{equation}

```{proof name="Proof"}
Example of a proof.

$$f\left(k\right)=\binom{n}{k}p^k\left(1-p\right)^{n-k}$$

$$f\left(k\right)=\binom{n}{k}p^k\left(p+1-2p\right)^{n-k}$$
```